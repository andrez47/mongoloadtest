<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :writer_id %><br />
    <%= f.collection_select :writer_id, Member.all, :id, :first_name %>
  </div>
  <div class="field">
    <%= f.label :member_recipient_id %><br />
    <%= f.collection_select :member_recipient_id, Member.all, :id, :first_name %>
  </div>
  <div class="field">
    <%= f.label :horse_partner_id %><br />
    <%= f.collection_select :horse_partner_id, Horse.all, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :horse_recipient_id %><br />
    <%= f.collection_select :horse_recipient_id, Horse.all, :id, :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
